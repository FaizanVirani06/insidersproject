# Copy this file to .env and fill in values.
#
# IMPORTANT:
# - Never commit real secrets (Stripe keys, JWT secret, API keys) to git.
# - In production, always use strong random passwords/secrets.

# -----------------
# Domain / TLS (Caddy)
# -----------------
DOMAIN=faizansayshi.com
CADDY_EMAIL=you@example.com

# -----------------
# Database
# -----------------
# Recommended: Postgres.
POSTGRES_PASSWORD=postgres
# In docker-compose.prod.yml the host should be "db".
# Local development (API running on your machine):
INSIDER_DATABASE_URL=postgresql://postgres:postgres@localhost:5432/insider_platform
# Docker Compose (containers talking to each other):
# INSIDER_DATABASE_URL=postgresql://postgres:postgres@db:5432/insider_platform

# If you still want to run SQLite locally (not recommended for production):
# INSIDER_DB_PATH=./insider_platform.sqlite

# -----------------
# SEC (EDGAR)
# -----------------
# Required by SEC for EDGAR requests (must be descriptive)
SEC_USER_AGENT=InsiderPlatform/0.1 (contact: you@example.com)

# -----------------
# Prices (EODHD)
# -----------------
EODHD_API_KEY=696948d50a4e98.04356583

# -----------------
# AI (Gemini)
# -----------------
GEMINI_API_KEY=AIzaSyAhyPorCxcmQRtj6Lp8IVQlJRaczzGI6NY
GEMINI_MODEL=gemini-2.5-flash-lite

# -----------------
# API
# -----------------
API_HOST=0.0.0.0
API_PORT=8000

# -----------------
# Worker / poller
# -----------------
ENABLE_FORM4_POLLER=0
FORM4_POLLER_INTERVAL_SECONDS=120

# -----------------
# Auth (JWT)
# -----------------
# In production, set this to a strong random value.
AUTH_JWT_SECRET=dev_change_me
AUTH_TOKEN_EXPIRE_MINUTES=10080

# Optional: bootstrap an initial admin user on first run (only if users table is empty)
# AUTH_BOOTSTRAP_ADMIN_USERNAME=admin@example.com
# AUTH_BOOTSTRAP_ADMIN_PASSWORD=change_me

# Cookie-based sessions
# If you want cookies to work on both faizansayshi.com and www.faizansayshi.com,
# set AUTH_COOKIE_DOMAIN=.faizansayshi.com
AUTH_COOKIE_DOMAIN=.faizansayshi.com
AUTH_COOKIE_SAMESITE=lax
# AUTH_COOKIE_SECURE=1   # optional override

# -----------------
# Billing (Stripe)
# -----------------
# Public URL of your site (used for Stripe Checkout redirects)
PUBLIC_APP_URL=https://faizansayshi.com

# Stripe secrets (create these in the Stripe dashboard)
STRIPE_SECRET_KEY=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
STRIPE_WEBHOOK_SECRET=whsec_VbLHXw00ZFvCRApC2PHHA7pxLbcrifVf

# Stripe price IDs for your subscription plans
STRIPE_PRICE_ID_MONTHLY=price_1T0RjODA366w5nSPVZizn5Tn
STRIPE_PRICE_ID_YEARLY=price_1T0RjdDA366w5nSPn5Uymepe

# Development helper: bypass subscription checks (0/1)
BILLING_DEV_BYPASS=0
